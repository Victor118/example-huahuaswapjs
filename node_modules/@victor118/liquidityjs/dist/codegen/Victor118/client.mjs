import { Registry } from "@cosmjs/proto-signing";
import { defaultRegistryTypes, AminoTypes, SigningStargateClient } from "@cosmjs/stargate";
import * as victor118LiquidityV1beta1TxRegistry from "./liquidity/v1beta1/tx.registry";
import * as victor118LiquidityV1beta1TxAmino from "./liquidity/v1beta1/tx.amino";
export const victor118AminoConverters = {
    ...victor118LiquidityV1beta1TxAmino.AminoConverter
};
export const victor118ProtoRegistry = [...victor118LiquidityV1beta1TxRegistry.registry];
export const getSigningVictor118ClientOptions = ({ defaultTypes = defaultRegistryTypes } = {}) => {
    const registry = new Registry([...defaultTypes, ...victor118ProtoRegistry]);
    const aminoTypes = new AminoTypes({
        ...victor118AminoConverters
    });
    return {
        registry,
        aminoTypes
    };
};
export const getSigningVictor118Client = async ({ rpcEndpoint, signer, defaultTypes = defaultRegistryTypes }) => {
    const { registry, aminoTypes } = getSigningVictor118ClientOptions({
        defaultTypes
    });
    const client = await SigningStargateClient.connectWithSigner(rpcEndpoint, signer, {
        registry: registry,
        aminoTypes
    });
    return client;
};
//# sourceMappingURL=client.js.map